"use client";

import { useState, useRef, useEffect } from 'react';
import Image from 'next/image';

// Mock Initial Data - Staff
const initialStaff = [
    {
        id: 1,
        name: 'Ana García',
        role: 'Master Stylist',
        email: 'ana@bellasalon.com',
        phone: '+51 912 345 678',
        bio: 'Especialista en cortes modernos y transformación de imagen con más de 10 años de experiencia.',
        specialties: ['Corte', 'Estilismo', 'Asesoría'],
        image: 'https://images.unsplash.com/photo-1595152772835-219674b2a8a6?auto=format&fit=crop&q=80',
        active: true,
        schedule: {
            weeklySchedule: {
                'Lun': { active: true, start: '09:00', end: '18:00', breaks: [{ start: '13:00', end: '14:00' }] },
                'Mar': { active: true, start: '09:00', end: '18:00', breaks: [{ start: '13:00', end: '14:00' }] },
                'Mié': { active: true, start: '09:00', end: '18:00', breaks: [{ start: '13:00', end: '14:00' }] },
                'Jue': { active: true, start: '09:00', end: '18:00', breaks: [{ start: '13:00', end: '14:00' }] },
                'Vie': { active: true, start: '09:00', end: '18:00', breaks: [{ start: '13:00', end: '14:00' }] },
                'Sáb': { active: false, start: '09:00', end: '14:00', breaks: [] },
                'Dom': { active: false, start: '09:00', end: '14:00', breaks: [] }
            },
            vacations: ['2024-12-30', '2024-12-31'],
            exceptions: [
                { date: '2024-12-24', start: '09:00', end: '13:00', reason: 'Medio día - Navidad' }
            ],
            worksHolidays: false
        }
    },
    {
        id: 2,
        name: 'Carlos Ruiz',
        role: 'Colorista Senior',
        email: 'carlos@bellasalon.com',
        phone: '+51 987 654 321',
        bio: 'Experto en balayage y corrección de color. Certificado por las mejores academias de Europa.',
        specialties: ['Color', 'Balayage', 'Tratamientos'],
        image: 'https://images.unsplash.com/photo-1534030347209-56781b48d20c?auto=format&fit=crop&q=80',
        active: true,
        schedule: {
            weeklySchedule: {
                'Lun': { active: false, start: '10:00', end: '19:00', breaks: [] },
                'Mar': { active: true, start: '10:00', end: '19:00', breaks: [{ start: '14:00', end: '15:00' }] },
                'Mié': { active: true, start: '10:00', end: '19:00', breaks: [{ start: '14:00', end: '15:00' }] },
                'Jue': { active: true, start: '10:00', end: '19:00', breaks: [{ start: '14:00', end: '15:00' }] },
                'Vie': { active: true, start: '10:00', end: '19:00', breaks: [{ start: '14:00', end: '15:00' }] },
                'Sáb': { active: true, start: '10:00', end: '16:00', breaks: [] },
                'Dom': { active: false, start: '10:00', end: '16:00', breaks: [] }
            },
            vacations: [],
            exceptions: [],
            worksHolidays: true
        }
    },
];

// Mock Initial Data - Appointments
const initialAppointments = [
    { id: 101, clientName: 'María Pérez', service: 'Corte y Estilo', staff: 'Ana García', date: '2023-12-24', time: '10:00', status: 'Confirmada', phone: '+51 900 000 001' },
    { id: 102, clientName: 'Juan Lopez', service: 'Limpieza Facial', staff: 'Elena Torres', date: '2023-12-24', time: '11:00', status: 'Pendiente', phone: '+51 900 000 002' },
    { id: 103, clientName: 'Sofia Diaz', service: 'Coloración', staff: 'Carlos Ruiz', date: '2023-12-25', time: '15:00', status: 'Cancelada', phone: '+51 900 000 003' },
];

const initialHolidays = [
    '2024-12-25',
    '2025-01-01'
];

// Mock Initial Data - Services
const initialServices = [
    {
        id: 1,
        name: 'Corte y Estilo',
        category: 'Cabello',
        description: 'Corte personalizado con lavado, secado y peinado profesional.',
        duration: 60,
        price: 80,
        image: 'https://images.unsplash.com/photo-1560066984-138dadb4c035?auto=format&fit=crop&q=80',
        active: true,
        assignedStaff: ['Ana García', 'Carlos Ruiz'],
        requirements: 'Cabello limpio preferiblemente',
        featured: true
    },
    {
        id: 2,
        name: 'Coloración Completa',
        category: 'Cabello',
        description: 'Coloración profesional con productos premium. Incluye tratamiento post-color.',
        duration: 120,
        price: 150,
        image: 'https://images.unsplash.com/photo-1522337360788-8b13dee7a37e?auto=format&fit=crop&q=80',
        active: true,
        assignedStaff: ['Carlos Ruiz'],
        requirements: 'Consulta previa recomendada',
        featured: true
    },
    {
        id: 3,
        name: 'Manicura Spa',
        category: 'Uñas',
        description: 'Manicura completa con exfoliación, masaje y esmaltado semipermanente.',
        duration: 45,
        price: 50,
        image: 'https://images.unsplash.com/photo-1604654894610-df63bc536371?auto=format&fit=crop&q=80',
        active: true,
        assignedStaff: [],
        requirements: '',
        featured: false
    },
    {
        id: 4,
        name: 'Facial Glow',
        category: 'Tratamientos Faciales',
        description: 'Limpieza profunda con extracción, mascarilla hidratante y masaje facial.',
        duration: 90,
        price: 120,
        image: 'https://images.unsplash.com/photo-1570172619644-dfd03ed5d881?auto=format&fit=crop&q=80',
        active: true,
        assignedStaff: [],
        requirements: 'Piel sin maquillaje',
        featured: true
    },
];

export default function Administracion() {
    const [isAuthenticated, setIsAuthenticated] = useState(false);
    const [password, setPassword] = useState('');
    const [activeTab, setActiveTab] = useState('appointments');
    const fileInputRef = useRef<HTMLInputElement>(null);

    // Data States
    const [staff, setStaff] = useState(initialStaff);
    const [appointments, setAppointments] = useState(initialAppointments);
    const [holidays, setHolidays] = useState<string[]>(initialHolidays);
    const [newDate, setNewDate] = useState('');
    const [schedule, setSchedule] = useState({ start: '09:00', end: '20:00' });
    const [whatsappNumber, setWhatsappNumber] = useState('+51 999 999 999');

    // Staff Schedule Editor State
    const [editingStaffId, setEditingStaffId] = useState<number | null>(null);
    const [showScheduleModal, setShowScheduleModal] = useState(false);

    const daysOfWeek = ['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom'];

    // New Staff Form State
    const [newStaff, setNewStaff] = useState({
        name: '',
        role: '',
        email: '',
        phone: '',
        bio: '',
        specialties: '',
        image: 'https://images.unsplash.com/photo-1580489944761-15a19d654956?auto=format&fit=crop&q=80',
    });

    // New Appointment Form State
    const [newAppt, setNewAppt] = useState({
        clientName: '',
        service: 'Corte y Estilo',
        staff: 'Ana García',
        date: '',
        time: '',
        phone: ''
    });

    useEffect(() => {
        const savedNumber = localStorage.getItem('salon_whatsapp');
        if (savedNumber) setWhatsappNumber(savedNumber);
    }, []);

    const handleLogin = (e: React.FormEvent) => {
        e.preventDefault();
        if (password === 'admin123') {
            setIsAuthenticated(true);
        } else {
            alert('Contraseña incorrecta');
        }
    };

    // --- Staff Logic ---
    const toggleStaffStatus = (id: number) => {
        setStaff(staff.map(s => s.id === id ? { ...s, active: !s.active } : s));
    };

    const handleNewStaffChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {
        setNewStaff({ ...newStaff, [e.target.name]: e.target.value });
    };

    const handleImageUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
        const file = e.target.files?.[0];
        if (file) {
            const reader = new FileReader();
            reader.onloadend = () => {
                setNewStaff({ ...newStaff, image: reader.result as string });
            };
            reader.readAsDataURL(file);
        }
    };

    const addStaff = (e: React.FormEvent) => {
        e.preventDefault();
        if (!newStaff.name || !newStaff.role) return;

        const defaultSchedule = {
            weeklySchedule: {
                'Lun': { active: true, start: '09:00', end: '18:00', breaks: [] },
                'Mar': { active: true, start: '09:00', end: '18:00', breaks: [] },
                'Mié': { active: true, start: '09:00', end: '18:00', breaks: [] },
                'Jue': { active: true, start: '09:00', end: '18:00', breaks: [] },
                'Vie': { active: true, start: '09:00', end: '18:00', breaks: [] },
                'Sáb': { active: false, start: '09:00', end: '14:00', breaks: [] },
                'Dom': { active: false, start: '09:00', end: '14:00', breaks: [] }
            },
            vacations: [],
            exceptions: [],
            worksHolidays: false
        };

        const newMember = {
            id: staff.length + 1,
            name: newStaff.name,
            role: newStaff.role,
            email: newStaff.email,
            phone: newStaff.phone,
            bio: newStaff.bio,
            specialties: newStaff.specialties.split(',').map(s => s.trim()),
            image: newStaff.image || 'https://images.unsplash.com/photo-1580489944761-15a19d654956?auto=format&fit=crop&q=80',
            active: true,
            schedule: defaultSchedule
        };

        setStaff([...staff, newMember]);
        setNewStaff({ name: '', role: '', email: '', phone: '', bio: '', specialties: '', image: '' });
        alert("Personal agregado. Ahora puedes configurar su calendario detallado.");
    };

    // Advanced Schedule Management
    const openScheduleEditor = (staffId: number) => {
        setEditingStaffId(staffId);
        setShowScheduleModal(true);
    };

    const updateStaffSchedule = (staffId: number, day: string, field: string, value: any) => {
        setStaff(staff.map(s => {
            if (s.id === staffId) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        weeklySchedule: {
                            ...s.schedule.weeklySchedule,
                            [day]: {
                                ...s.schedule.weeklySchedule[day],
                                [field]: value
                            }
                        }
                    }
                };
            }
            return s;
        }));
    };

    const addVacationDay = (staffId: number, date: string) => {
        setStaff(staff.map(s => {
            if (s.id === staffId && date && !s.schedule.vacations.includes(date)) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        vacations: [...s.schedule.vacations, date].sort()
                    }
                };
            }
            return s;
        }));
    };

    const removeVacationDay = (staffId: number, date: string) => {
        setStaff(staff.map(s => {
            if (s.id === staffId) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        vacations: s.schedule.vacations.filter(d => d !== date)
                    }
                };
            }
            return s;
        }));
    };

    const addException = (staffId: number, exception: any) => {
        setStaff(staff.map(s => {
            if (s.id === staffId) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        exceptions: [...s.schedule.exceptions, exception]
                    }
                };
            }
            return s;
        }));
    };

    const removeException = (staffId: number, index: number) => {
        setStaff(staff.map(s => {
            if (s.id === staffId) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        exceptions: s.schedule.exceptions.filter((_, i) => i !== index)
                    }
                };
            }
            return s;
        }));
    };

    const toggleWorksHolidays = (staffId: number) => {
        setStaff(staff.map(s => {
            if (s.id === staffId) {
                return {
                    ...s,
                    schedule: {
                        ...s.schedule,
                        worksHolidays: !s.schedule.worksHolidays
                    }
                };
            }
            return s;
        }));
    };

    // --- Appointment Logic ---
    const handleApptChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => {
        setNewAppt({ ...newAppt, [e.target.name]: e.target.value });
    };

    const addAppointment = (e: React.FormEvent) => {
        e.preventDefault();
        const newId = Math.max(...appointments.map(a => a.id)) + 1;
        const apptToAdd = {
            id: newId,
            ...newAppt,
            status: 'Confirmada'
        };
        setAppointments([apptToAdd, ...appointments]);
        setNewAppt({ clientName: '', service: 'Corte y Estilo', staff: 'Ana García', date: '', time: '', phone: '' });
        alert("Cita agendada correctamente");
    };

    const updateApptStatus = (id: number, newStatus: string) => {
        setAppointments(appointments.map(a => a.id === id ? { ...a, status: newStatus } : a));
    };

    const deleteAppointment = (id: number) => {
        if (confirm('¿Estás seguro de eliminar esta cita?')) {
            setAppointments(appointments.filter(a => a.id !== id));
        }
    };

    // --- Schedule/Holiday Logic ---
    const addHoliday = () => {
        if (newDate && !holidays.includes(newDate)) {
            setHolidays([...holidays, newDate]);
            setNewDate('');
        }
    };

    const removeHoliday = (date: string) => {
        setHolidays(holidays.filter(h => h !== date));
    };

    // --- Settings Logic ---
    const saveSettings = () => {
        localStorage.setItem('salon_whatsapp', whatsappNumber);
        alert('Configuración guardada. El botón de WhatsApp se actualizará.');
    };

    if (!isAuthenticated) {
        return (
            <div className="min-h-screen bg-background flex items-center justify-center px-4">
                <div className="max-w-md w-full bg-white p-8 rounded-2xl shadow-xl border border-secondary/20">
                    <div className="text-center mb-8">
                        <h1 className="text-3xl font-serif text-primary mb-2">Administración</h1>
                        <p className="text-foreground/60">Acceso exclusivo para gerencia</p>
                    </div>
                    <form onSubmit={handleLogin} className="space-y-6">
                        <div>
                            <label className="block text-sm font-medium text-foreground/70 mb-2">Contraseña</label>
                            <input
                                type="password"
                                value={password}
                                onChange={(e) => setPassword(e.target.value)}
                                className="w-full px-4 py-3 rounded-xl border border-secondary focus:border-primary focus:ring-1 focus:ring-primary outline-none transition-colors"
                                placeholder="••••••••"
                            />
                        </div>
                        <button type="submit" className="w-full bg-primary text-white font-medium py-3 rounded-xl hover:bg-primary/90 transition-all">
                            Ingresar
                        </button>
                    </form>
                </div>
            </div>
        );
    }

    const editingStaff = staff.find(s => s.id === editingStaffId);

    return (
        <div className="min-h-screen bg-secondary/10 flex">
            {/* Sidebar */}
            <aside className="w-64 bg-white border-r border-secondary/20 hidden md:block fixed h-full z-10 overflow-y-auto">
                <div className="p-8">
                    <h2 className="text-2xl font-serif text-primary font-bold">BellaAdmin</h2>
                </div>
                <nav className="mt-4">
                    <button onClick={() => setActiveTab('appointments')} className={`w-full text-left px-8 py-4 font-medium transition-colors ${activeTab === 'appointments' ? 'bg-secondary/20 text-primary border-r-4 border-primary' : 'text-foreground/60 hover:bg-secondary/10'}`}>
                        📝 Citas y Agenda
                    </button>
                    <button onClick={() => setActiveTab('staff')} className={`w-full text-left px-8 py-4 font-medium transition-colors ${activeTab === 'staff' ? 'bg-secondary/20 text-primary border-r-4 border-primary' : 'text-foreground/60 hover:bg-secondary/10'}`}>
                        👥 Gestionar Personal
                    </button>
                    <button onClick={() => setActiveTab('schedule')} className={`w-full text-left px-8 py-4 font-medium transition-colors ${activeTab === 'schedule' ? 'bg-secondary/20 text-primary border-r-4 border-primary' : 'text-foreground/60 hover:bg-secondary/10'}`}>
                        📅 Horarios y Festivos
                    </button>
                    <button onClick={() => setActiveTab('settings')} className={`w-full text-left px-8 py-4 font-medium transition-colors ${activeTab === 'settings' ? 'bg-secondary/20 text-primary border-r-4 border-primary' : 'text-foreground/60 hover:bg-secondary/10'}`}>
                        ⚙️ Configuración
                    </button>
                    <button onClick={() => setActiveTab('stats')} className={`w-full text-left px-8 py-4 font-medium transition-colors ${activeTab === 'stats' ? 'bg-secondary/20 text-primary border-r-4 border-primary' : 'text-foreground/60 hover:bg-secondary/10'}`}>
                        📊 Estadísticas
                    </button>
                </nav>
                <div className="p-8 border-t border-secondary/20 mt-auto">
                    <button onClick={() => setIsAuthenticated(false)} className="text-red-500 font-medium hover:text-red-600 flex items-center gap-2">
                        ← Cerrar Sesión
                    </button>
                </div>
            </aside>

            {/* Advanced Schedule Modal */}
            {showScheduleModal && editingStaff && (
                <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4 overflow-y-auto">
                    <div className="bg-white rounded-2xl shadow-2xl max-w-5xl w-full max-h-[90vh] overflow-y-auto">
                        <div className="sticky top-0 bg-white border-b border-secondary/20 p-6 flex justify-between items-center">
                            <div>
                                <h2 className="text-2xl font-serif font-bold text-foreground">Calendario de {editingStaff.name}</h2>
                                <p className="text-sm text-foreground/60">{editingStaff.role}</p>
                            </div>
                            <button onClick={() => setShowScheduleModal(false)} className="text-3xl text-foreground/40 hover:text-foreground transition-colors">×</button>
                        </div>

                        <div className="p-6 space-y-8">
                            {/* Weekly Schedule */}
                            <div>
                                <h3 className="text-lg font-bold text-foreground mb-4 flex items-center gap-2">
                                    📅 Horario Semanal Regular
                                </h3>
                                <div className="space-y-3">
                                    {daysOfWeek.map(day => {
                                        const daySchedule = editingStaff.schedule.weeklySchedule[day];
                                        return (
                                            <div key={day} className={`p-4 rounded-xl border-2 transition-all ${daySchedule.active ? 'bg-green-50 border-green-200' : 'bg-gray-50 border-gray-200'}`}>
                                                <div className="flex items-center gap-4 flex-wrap">
                                                    <div className="flex items-center gap-3">
                                                        <input
                                                            type="checkbox"
                                                            checked={daySchedule.active}
                                                            onChange={(e) => updateStaffSchedule(editingStaff.id, day, 'active', e.target.checked)}
                                                            className="w-5 h-5 accent-primary cursor-pointer"
                                                        />
                                                        <span className="font-bold text-foreground w-12">{day}</span>
                                                    </div>

                                                    {daySchedule.active && (
                                                        <>
                                                            <div className="flex items-center gap-2">
                                                                <label className="text-sm text-foreground/70">Inicio:</label>
                                                                <input
                                                                    type="time"
                                                                    value={daySchedule.start}
                                                                    onChange={(e) => updateStaffSchedule(editingStaff.id, day, 'start', e.target.value)}
                                                                    className="px-3 py-1 rounded-lg border border-secondary text-sm"
                                                                />
                                                            </div>
                                                            <div className="flex items-center gap-2">
                                                                <label className="text-sm text-foreground/70">Fin:</label>
                                                                <input
                                                                    type="time"
                                                                    value={daySchedule.end}
                                                                    onChange={(e) => updateStaffSchedule(editingStaff.id, day, 'end', e.target.value)}
                                                                    className="px-3 py-1 rounded-lg border border-secondary text-sm"
                                                                />
                                                            </div>
                                                            <span className="text-xs text-foreground/50 ml-auto">
                                                                {(() => {
                                                                    const start = new Date(`2000-01-01T${daySchedule.start}`);
                                                                    const end = new Date(`2000-01-01T${daySchedule.end}`);
                                                                    const hours = (end.getTime() - start.getTime()) / (1000 * 60 * 60);
                                                                    return `${hours.toFixed(1)}h`;
                                                                })()}
                                                            </span>
                                                        </>
                                                    )}
                                                </div>
                                            </div>
                                        );
                                    })}
                                </div>
                            </div>

                            {/* Vacations/Days Off */}
                            <div>
                                <h3 className="text-lg font-bold text-foreground mb-4 flex items-center gap-2">
                                    🏖️ Vacaciones y Días Libres
                                </h3>
                                <div className="bg-secondary/5 p-4 rounded-xl">
                                    <div className="flex gap-2 mb-4">
                                        <input
                                            type="date"
                                            id={`vacation-${editingStaff.id}`}
                                            className="flex-1 px-4 py-2 rounded-lg border border-secondary"
                                        />
                                        <button
                                            onClick={() => {
                                                const input = document.getElementById(`vacation-${editingStaff.id}`) as HTMLInputElement;
                                                if (input.value) {
                                                    addVacationDay(editingStaff.id, input.value);
                                                    input.value = '';
                                                }
                                            }}
                                            className="bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary/90 font-medium"
                                        >
                                            + Agregar Día
                                        </button>
                                    </div>
                                    <div className="space-y-2 max-h-48 overflow-y-auto">
                                        {editingStaff.schedule.vacations.length === 0 && (
                                            <p className="text-sm text-foreground/40 italic text-center py-4">No hay días de vacaciones registrados</p>
                                        )}
                                        {editingStaff.schedule.vacations.map(date => (
                                            <div key={date} className="flex justify-between items-center p-3 bg-white rounded-lg border border-secondary/20">
                                                <span className="font-medium text-foreground">{date}</span>
                                                <button
                                                    onClick={() => removeVacationDay(editingStaff.id, date)}
                                                    className="text-red-500 hover:text-red-700 text-sm font-bold"
                                                >
                                                    Eliminar
                                                </button>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            </div>

                            {/* Exceptions */}
                            <div>
                                <h3 className="text-lg font-bold text-foreground mb-4 flex items-center gap-2">
                                    ⚡ Excepciones (Horarios Especiales)
                                </h3>
                                <div className="bg-secondary/5 p-4 rounded-xl">
                                    <div className="grid grid-cols-1 md:grid-cols-4 gap-2 mb-4">
                                        <input
                                            type="date"
                                            id={`exc-date-${editingStaff.id}`}
                                            className="px-4 py-2 rounded-lg border border-secondary"
                                            placeholder="Fecha"
                                        />
                                        <input
                                            type="time"
                                            id={`exc-start-${editingStaff.id}`}
                                            className="px-4 py-2 rounded-lg border border-secondary"
                                            placeholder="Inicio"
                                        />
                                        <input
                                            type="time"
                                            id={`exc-end-${editingStaff.id}`}
                                            className="px-4 py-2 rounded-lg border border-secondary"
                                            placeholder="Fin"
                                        />
                                        <button
                                            onClick={() => {
                                                const dateInput = document.getElementById(`exc-date-${editingStaff.id}`) as HTMLInputElement;
                                                const startInput = document.getElementById(`exc-start-${editingStaff.id}`) as HTMLInputElement;
                                                const endInput = document.getElementById(`exc-end-${editingStaff.id}`) as HTMLInputElement;

                                                if (dateInput.value && startInput.value && endInput.value) {
                                                    addException(editingStaff.id, {
                                                        date: dateInput.value,
                                                        start: startInput.value,
                                                        end: endInput.value,
                                                        reason: 'Horario especial'
                                                    });
                                                    dateInput.value = '';
                                                    startInput.value = '';
                                                    endInput.value = '';
                                                }
                                            }}
                                            className="bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary/90 font-medium"
                                        >
                                            + Agregar
                                        </button>
                                    </div>
                                    <div className="space-y-2 max-h-48 overflow-y-auto">
                                        {editingStaff.schedule.exceptions.length === 0 && (
                                            <p className="text-sm text-foreground/40 italic text-center py-4">No hay excepciones configuradas</p>
                                        )}
                                        {editingStaff.schedule.exceptions.map((exc, idx) => (
                                            <div key={idx} className="flex justify-between items-center p-3 bg-yellow-50 rounded-lg border border-yellow-200">
                                                <div>
                                                    <span className="font-bold text-foreground">{exc.date}</span>
                                                    <span className="text-sm text-foreground/70 ml-3">{exc.start} - {exc.end}</span>
                                                    {exc.reason && <span className="text-xs text-foreground/50 ml-2">({exc.reason})</span>}
                                                </div>
                                                <button
                                                    onClick={() => removeException(editingStaff.id, idx)}
                                                    className="text-red-500 hover:text-red-700 text-sm font-bold"
                                                >
                                                    Eliminar
                                                </button>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            </div>

                            {/* Works Holidays Toggle */}
                            <div className="bg-secondary/5 p-4 rounded-xl">
                                <div className="flex items-center gap-3">
                                    <input
                                        type="checkbox"
                                        id={`holidays-${editingStaff.id}`}
                                        checked={editingStaff.schedule.worksHolidays}
                                        onChange={() => toggleWorksHolidays(editingStaff.id)}
                                        className="w-5 h-5 accent-primary cursor-pointer"
                                    />
                                    <label htmlFor={`holidays-${editingStaff.id}`} className="text-sm font-medium text-foreground cursor-pointer">
                                        ⭐ Disponible para trabajar en días festivos nacionales
                                    </label>
                                </div>
                            </div>

                            <div className="flex justify-end pt-4 border-t border-secondary/20">
                                <button
                                    onClick={() => {
                                        setShowScheduleModal(false);
                                        alert('Calendario actualizado correctamente');
                                    }}
                                    className="bg-primary text-white px-8 py-3 rounded-xl hover:bg-primary/90 font-medium shadow-lg"
                                >
                                    Guardar Cambios
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            )}

            {/* Main Content */}
            <main className="flex-1 p-8 md:ml-64 overflow-y-auto">
                <div className="max-w-6xl mx-auto">

                    {/* Header Mobile */}
                    <div className="md:hidden flex justify-between items-center mb-8">
                        <h1 className="text-2xl font-serif text-primary">Admin Panel</h1>
                        <button onClick={() => setIsAuthenticated(false)} className="text-sm text-red-500">Salir</button>
                    </div>

                    {/* --- APPOINTMENTS TAB --- */}
                    {activeTab === 'appointments' && (
                        <div className="space-y-8 animate-fade-in-up">
                            <h2 className="text-3xl font-serif text-foreground mb-6">Gestión de Citas</h2>

                            {/* Add Appointment Form */}
                            <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20 mb-8">
                                <h3 className="text-lg font-bold text-foreground mb-4">Agendar Nueva Cita</h3>
                                <form onSubmit={addAppointment} className="grid grid-cols-1 md:grid-cols-3 gap-4">
                                    <input required name="clientName" placeholder="Cliente" value={newAppt.clientName} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary" />
                                    <input required name="phone" placeholder="Teléfono" value={newAppt.phone} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary" />
                                    <select name="service" value={newAppt.service} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary">
                                        <option>Corte y Estilo</option>
                                        <option>Coloración</option>
                                        <option>Manicura Spa</option>
                                        <option>Facial Glow</option>
                                    </select>
                                    <select name="staff" value={newAppt.staff} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary">
                                        {staff.filter(s => s.active).map(s => <option key={s.id} value={s.name}>{s.name}</option>)}
                                    </select>
                                    <input required name="date" type="date" value={newAppt.date} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary" />
                                    <input required name="time" type="time" value={newAppt.time} onChange={handleApptChange} className="px-4 py-2 rounded-lg border border-secondary" />
                                    <button type="submit" className="md:col-span-3 bg-primary text-white py-2 rounded-lg hover:bg-primary/90 font-medium">Agendar Cita</button>
                                </form>
                            </div>

                            {/* Appointments List */}
                            <div className="bg-white rounded-2xl shadow-sm border border-secondary/20 overflow-hidden">
                                <table className="w-full text-left">
                                    <thead className="bg-secondary/10 text-foreground/70">
                                        <tr>
                                            <th className="p-4 font-medium">Cliente</th>
                                            <th className="p-4 font-medium">Servicio</th>
                                            <th className="p-4 font-medium">Especialista</th>
                                            <th className="p-4 font-medium">Fecha/Hora</th>
                                            <th className="p-4 font-medium">Estado</th>
                                            <th className="p-4 font-medium">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody className="divide-y divide-secondary/10">
                                        {appointments.length === 0 && <tr><td colSpan={6} className="p-8 text-center text-foreground/40">No hay citas registradas.</td></tr>}
                                        {appointments.map((appt) => (
                                            <tr key={appt.id} className="hover:bg-secondary/5 transition-colors">
                                                <td className="p-4">
                                                    <div className="font-bold text-foreground">{appt.clientName}</div>
                                                    <div className="text-xs text-foreground/50">{appt.phone}</div>
                                                </td>
                                                <td className="p-4 text-sm">{appt.service}</td>
                                                <td className="p-4 text-sm">{appt.staff}</td>
                                                <td className="p-4 text-sm">
                                                    <div>{appt.date}</div>
                                                    <div className="text-xs text-primary font-bold">{appt.time}</div>
                                                </td>
                                                <td className="p-4">
                                                    <span className={`px-2 py-1 rounded-full text-xs font-bold 
                                                        ${appt.status === 'Confirmada' ? 'bg-green-100 text-green-700' :
                                                            appt.status === 'Pendiente' ? 'bg-yellow-100 text-yellow-700' :
                                                                'bg-red-100 text-red-700'}`}>
                                                        {appt.status}
                                                    </span>
                                                </td>
                                                <td className="p-4">
                                                    <div className="flex gap-2">
                                                        <button onClick={() => updateApptStatus(appt.id, 'Confirmada')} className="text-xs bg-green-50 text-green-600 px-2 py-1 rounded hover:bg-green-100" title="Confirmar">✓</button>
                                                        <button onClick={() => updateApptStatus(appt.id, 'Pendiente')} className="text-xs bg-yellow-50 text-yellow-600 px-2 py-1 rounded hover:bg-yellow-100" title="Pendiente">⏳</button>
                                                        <button onClick={() => updateApptStatus(appt.id, 'Cancelada')} className="text-xs bg-red-50 text-red-600 px-2 py-1 rounded hover:bg-red-100" title="Cancelar">✕</button>
                                                        <button onClick={() => deleteAppointment(appt.id)} className="text-xs bg-gray-50 text-gray-600 px-2 py-1 rounded hover:bg-gray-100" title="Eliminar">🗑</button>
                                                    </div>
                                                </td>
                                            </tr>
                                        ))}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    )}

                    {/* --- STAFF TAB --- */}
                    {activeTab === 'staff' && (
                        <div className="space-y-8 animate-fade-in-up">
                            <h2 className="text-3xl font-serif text-foreground mb-6">Nuestro Equipo</h2>

                            {/* Add Staff Form */}
                            <div className="bg-white p-8 rounded-2xl shadow-sm border border-secondary/20">
                                <h3 className="text-lg font-bold text-foreground mb-6">Agregar Nuevo Especialista</h3>
                                <form onSubmit={addStaff} className="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div className="space-y-4">
                                        <input name="name" type="text" placeholder="Nombre completo *" value={newStaff.name} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" required />
                                        <input name="role" type="text" placeholder="Cargo / Rol *" value={newStaff.role} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" required />
                                        <input name="email" type="email" placeholder="Email" value={newStaff.email} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />
                                    </div>
                                    <div className="space-y-4">
                                        <input name="phone" type="tel" placeholder="Teléfono" value={newStaff.phone} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />
                                        <input name="specialties" type="text" placeholder="Especialidades (separadas por coma)" value={newStaff.specialties} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />

                                        <div className="flex items-center gap-4">
                                            <div className="w-12 h-12 relative rounded-full overflow-hidden bg-secondary/20 border border-secondary">
                                                <Image src={newStaff.image || 'https://images.unsplash.com/photo-1580489944761-15a19d654956?auto=format&fit=crop&q=80'} alt="Preview" fill className="object-cover" />
                                            </div>
                                            <input type="file" ref={fileInputRef} onChange={handleImageUpload} className="hidden" accept="image/*" />
                                            <button type="button" onClick={() => fileInputRef.current?.click()} className="px-4 py-2 bg-secondary/20 hover:bg-secondary/40 text-foreground rounded-lg text-sm font-medium transition-colors flex items-center gap-2">
                                                <span>📷</span> Subir Foto
                                            </button>
                                        </div>
                                    </div>
                                    <div className="md:col-span-2">
                                        <textarea name="bio" placeholder="Biografía..." rows={3} value={newStaff.bio} onChange={handleNewStaffChange} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none resize-none"></textarea>
                                    </div>
                                    <div className="md:col-span-2">
                                        <button type="submit" className="w-full md:w-auto bg-primary text-white px-8 py-3 rounded-lg hover:bg-primary/90 shadow-lg">Guardar Nuevo Especialista</button>
                                    </div>
                                </form>
                            </div>

                            {/* Staff List */}
                            <div className="grid grid-cols-1 gap-6">
                                {staff.map((member) => (
                                    <div key={member.id} className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20 flex flex-col md:flex-row gap-6 items-start hover:shadow-md transition-shadow">
                                        <div className="w-full md:w-32 h-32 relative rounded-xl overflow-hidden flex-shrink-0 bg-gray-100">
                                            <Image src={member.image || 'https://images.unsplash.com/photo-1580489944761-15a19d654956?auto=format&fit=crop&q=80'} alt={member.name} fill className="object-cover" />
                                        </div>
                                        <div className="flex-1 w-full">
                                            <div className="flex justify-between items-start mb-2">
                                                <div>
                                                    <h3 className="text-xl font-serif font-bold text-foreground flex items-center gap-3">
                                                        {member.name}
                                                        <span className={`px-2 py-0.5 rounded-full text-xs font-bold uppercase tracking-wider ${member.active ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700'}`}>{member.active ? 'Activo' : 'Inactivo'}</span>
                                                    </h3>
                                                    <p className="text-primary font-medium">{member.role}</p>
                                                </div>
                                                <div className="flex gap-2">
                                                    <button
                                                        onClick={() => openScheduleEditor(member.id)}
                                                        className="px-4 py-2 bg-primary text-white rounded-lg text-sm font-medium hover:bg-primary/90 transition-colors flex items-center gap-2"
                                                    >
                                                        📅 Calendario
                                                    </button>
                                                    <button onClick={() => toggleStaffStatus(member.id)} className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${member.active ? 'bg-secondary/20 text-foreground hover:bg-secondary/40' : 'bg-primary text-white hover:bg-primary/90'}`}>{member.active ? 'Desactivar' : 'Reactivar'}</button>
                                                </div>
                                            </div>
                                            <p className="text-foreground/70 text-sm mb-4 leading-relaxed">{member.bio}</p>
                                            <div className="flex flex-wrap gap-2 mb-4">
                                                {member.specialties.map((spec, idx) => (<span key={idx} className="bg-secondary/30 text-foreground/80 px-2 py-1 rounded text-xs font-medium">{spec}</span>))}
                                            </div>

                                            {/* Quick Schedule Summary */}
                                            {member.schedule && (
                                                <div className="mt-4 pt-4 border-t border-secondary/20">
                                                    <div className="flex flex-wrap items-center gap-4 text-sm text-foreground/70">
                                                        <div className="flex items-center gap-2">
                                                            <span className="text-lg">📅</span>
                                                            <span className="font-bold">
                                                                {Object.entries(member.schedule.weeklySchedule)
                                                                    .filter(([_, schedule]) => schedule.active)
                                                                    .map(([day]) => day)
                                                                    .join(', ')}
                                                            </span>
                                                        </div>
                                                        {member.schedule.vacations.length > 0 && (
                                                            <div className="flex items-center gap-2 text-orange-600">
                                                                <span>🏖️</span>
                                                                <span className="text-xs">{member.schedule.vacations.length} día(s) de vacaciones</span>
                                                            </div>
                                                        )}
                                                        {member.schedule.exceptions.length > 0 && (
                                                            <div className="flex items-center gap-2 text-yellow-600">
                                                                <span>⚡</span>
                                                                <span className="text-xs">{member.schedule.exceptions.length} excepción(es)</span>
                                                            </div>
                                                        )}
                                                        {member.schedule.worksHolidays && (
                                                            <div className="flex items-center gap-2 text-green-600 font-bold text-xs bg-green-50 px-2 py-1 rounded-full">
                                                                ⭐ Trabaja Feriados
                                                            </div>
                                                        )}
                                                    </div>
                                                </div>
                                            )}
                                        </div>
                                    </div>
                                ))}
                            </div>
                        </div>
                    )}

                    {/* --- SCHEDULE TAB --- */}
                    {activeTab === 'schedule' && (
                        <div className="space-y-8 animate-fade-in-up">
                            <h2 className="text-3xl font-serif text-foreground mb-6">Configuración de Agenda</h2>
                            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20">
                                    <h3 className="text-lg font-bold text-foreground mb-6">Horario Laboral General</h3>
                                    <div className="space-y-4">
                                        <div>
                                            <label className="block text-sm font-medium text-foreground/70 mb-2">Apertura</label>
                                            <input type="time" value={schedule.start} onChange={(e) => setSchedule({ ...schedule, start: e.target.value })} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />
                                        </div>
                                        <div>
                                            <label className="block text-sm font-medium text-foreground/70 mb-2">Cierre</label>
                                            <input type="time" value={schedule.end} onChange={(e) => setSchedule({ ...schedule, end: e.target.value })} className="w-full px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />
                                        </div>
                                        <button className="w-full bg-primary text-white py-2 rounded-lg mt-4 hover:bg-primary/90">Guardar Horario</button>
                                    </div>
                                </div>
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20">
                                    <h3 className="text-lg font-bold text-foreground mb-6">Días No Laborables (Feriados Nacionales)</h3>
                                    <div className="flex gap-2 mb-6">
                                        <input type="date" value={newDate} onChange={(e) => setNewDate(e.target.value)} className="flex-1 px-4 py-2 rounded-lg border border-secondary focus:border-primary outline-none" />
                                        <button onClick={addHoliday} className="bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary/90">Bloquear Fecha</button>
                                    </div>
                                    <div className="space-y-2 max-h-64 overflow-y-auto">
                                        {holidays.length === 0 && <p className="text-sm text-foreground/40 italic">No hay días bloqueados.</p>}
                                        {holidays.map((date) => (
                                            <div key={date} className="flex justify-between items-center p-3 bg-secondary/10 rounded-lg">
                                                <span className="font-medium text-foreground">{date}</span>
                                                <button onClick={() => removeHoliday(date)} className="text-red-500 hover:text-red-700 text-sm font-bold">Eliminar</button>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            </div>
                        </div>
                    )}

                    {/* --- SETTINGS TAB --- */}
                    {activeTab === 'settings' && (
                        <div className="space-y-8 animate-fade-in-up">
                            <h2 className="text-3xl font-serif text-foreground mb-6">Configuración General</h2>
                            <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20 max-w-2xl">
                                <h3 className="text-lg font-bold text-foreground mb-6">Contacto y Redes</h3>
                                <div className="space-y-6">
                                    <div>
                                        <label className="block text-sm font-medium text-foreground/70 mb-2">Número de WhatsApp Principal</label>
                                        <p className="text-xs text-foreground/50 mb-2">El botón flotante de WhatsApp dirigirá a este número.</p>
                                        <input
                                            type="text"
                                            value={whatsappNumber}
                                            onChange={(e) => setWhatsappNumber(e.target.value)}
                                            className="w-full px-4 py-3 rounded-xl border border-secondary focus:border-primary outline-none"
                                            placeholder="+51 999 999 999"
                                        />
                                    </div>
                                    <button onClick={saveSettings} className="bg-primary text-white px-8 py-3 rounded-xl hover:bg-primary/90 transition-all font-medium">
                                        Guardar Configuración
                                    </button>
                                </div>
                            </div>
                        </div>
                    )}

                    {/* --- STATS TAB --- */}
                    {activeTab === 'stats' && (
                        <div className="animate-fade-in-up">
                            <h2 className="text-3xl font-serif text-foreground mb-6">Estadísticas del Salón</h2>
                            <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                                <div className="bg-primary text-white p-6 rounded-2xl shadow-lg">
                                    <h3 className="text-lg opacity-80 mb-2">Citas Hoy</h3>
                                    <p className="text-4xl font-bold">{appointments.filter(a => a.date === new Date().toISOString().split('T')[0]).length}</p>
                                </div>
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20">
                                    <h3 className="text-lg text-foreground/60 mb-2">Citas Totales</h3>
                                    <p className="text-4xl font-bold text-foreground">{appointments.length}</p>
                                </div>
                                <div className="bg-white p-6 rounded-2xl shadow-sm border border-secondary/20">
                                    <h3 className="text-lg text-foreground/60 mb-2">Personal Activo</h3>
                                    <p className="text-4xl font-bold text-foreground">{staff.filter(s => s.active).length}</p>
                                </div>
                            </div>
                        </div>
                    )}

                </div>
            </main>
        </div>
    );
}
